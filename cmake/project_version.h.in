// 由 CMake configure_file 自动生成，请勿手工修改。
// 集中固化项目版本与「锁定的上游 Mooncake 版本」，供 C++ 与绑定层在编译期引用。
#ifndef PROJECT_VERSION_H_
#define PROJECT_VERSION_H_

namespace project {

// 本项目语义化版本号。
inline constexpr const char* kProjectVersion = "@PROJECT_VERSION@";

// mooncake.lock 锁定的上游 Mooncake 版本与 commit（需求 1.3 / 6.3）。
inline constexpr const char* kMooncakeRequiredVersion = "@MOONCAKE_REQUIRED_VERSION@";
inline constexpr const char* kMooncakeRequiredCommit  = "@MOONCAKE_REQUIRED_COMMIT@";

}  // namespace project

#endif  // PROJECT_VERSION_H_
